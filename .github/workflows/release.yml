name: Release

on:
  push:
    tags: ['v*.*.*']

jobs:
  release:
    uses: Cupkek-Games/.github/.github/workflows/upm-release.yml@main
    secrets: inherit
    permissions:
      contents: write
